name: Java Maven Build

on:
  push:
    branches: [ master ]
    paths-ignore:
      - '**/README.md'
  pull_request:
    branches: [ master ]
    paths-ignore:
      - '**/README.md'

jobs:
  call-workflow:
    uses: fuinorg/github-workflows/.github/workflows/maven.yml@main
    with:
      oss_sonatype_user: michael-schnell
