name: Java Maven Build

on:
  push:
    branches: [ master ]
    paths-ignore:
      - '**/README.md'
  pull_request:
    branches: [ master ]
    paths-ignore:
      - '**/README.md'

jobs:
  maven_build:
    uses: fuinorg/github-workflows/.github/workflows/java-maven-sonar-oss.yml@main
    with:
      oss_sonatype_user: michael-schnell
