name: Java Maven Build

on:
  push:
    branches: [ master ]
    paths-ignore:
      - '**/README.md'
  pull_request:
    branches: [ master ]
    paths-ignore:
      - '**/README.md'

jobs:
  call-workflow-2:
    uses: fuinorg/github-workflows/.github/workflows/maven.yml@d2c87f1df5d25e38de1dad63927e109f3182ebac
    with:
      oss_sonatype_user: michael-schnell
